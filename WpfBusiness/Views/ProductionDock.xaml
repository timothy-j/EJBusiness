<UserControl x:Class="ProductionDock"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfBusiness"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="150">
    <UserControl.Resources>
        <Style TargetType="Button">
            <Setter Property="Command" Value="{Binding OpenMachineRoutedCommand}"/>
            <Setter Property="CommandParameter" Value="{Binding Content, RelativeSource={RelativeSource Self}}"/>
            <Setter Property="Margin" Value="3"/>
        </Style>
    </UserControl.Resources>
    <StackPanel Margin="3">
        <Button Content="Orders" Command="{Binding OpenOrdersRoutedCommand}"/>
        <Button Content="Inspection" Command="{Binding OpenInspectionCommand}"/>
        <Button Content="Required Parts" Command="{Binding OpenRequiredPartsCommand}"/>
    </StackPanel>
</UserControl>
